<h1>EAT-DA-BURGER <i class="fas fa-hamburger"></i></h1>



<h2>Burgers that have not been eaten</h2>

<ol>
    {{#each burgers}}
      {{#unless devoured}}
        {{> burgers/burgers-block}}
      {{/unless}}
    {{/each}}
</ol>

<h2>Burgers that have been eaten</h2>

<ol>
    {{#each burgers}}
      {{#if devoured}}
        {{> burgers/burgers-block-Eaten}}
      {{/if}}
    {{/each}}
</ol>


<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger_name">Burger Name:</label>
    <input type="text" id="burger_name" name="burger_name">
  </div>

  <button type="submit" class="addBtn">Add Burger</button>
</form>